# 승격 조건과 운영 규칙 — 자세한 근거

메인 SKILL.md의 절차만으로 판단이 애매할 때, 또는 스킬 예산·감가상각을 확인할 때만 이 파일을 읽는다.

## 목차

- [왜 신중해야 하나](#왜-신중해야-하나)
- [5개 조건 상세](#5개-조건-상세)
- [스킬 예산](#스킬-예산)
- [감가상각](#감가상각)
- [작성 규칙](#작성-규칙)
- [효과 판정 상세](#효과-판정-상세)

## 왜 신중해야 하나

스킬은 공짜가 아니다. **description은 매 프롬프트마다 읽힌다.** 만들기만 하면 6개월 뒤 스킬 자체가 낭비 원인이 된다.

공개 skill 55,315개 분석에서 26.4%는 라우팅 설명이 아예 없었고, body의 60% 이상이 실행에 쓰이지 않는 내용이었다(SkillReducer). 설명 48%·본문 39%를 줄였을 때 기능 품질은 오히려 2.8% 올랐다.

## 5개 조건 상세

| # | 조건 | 확인 방법 |
|---|---|---|
| 1 | 30일 내 5회 이상 | `promote --list`가 보여주는 횟수 (미달이면 CLI가 자체적으로 거부한다) |
| 2 | 원인 신호가 같다 | 같은 명령으로 묶인 코드들인지 `--list` 출력에서 확인 |
| 3 | 예상 절감 > 상시 비용 × 30일 | 상시 비용 = description 토큰 × 하루 프롬프트 수 × 30. 이 계산을 사용자에게 한 줄로 보여주고 판단을 구한다 — CLI는 이 판단을 하지 않는다 |
| 4 | description ≤ 500자, 본문 ≤ 60줄 | `promote` 실행 시 자동으로 강제된다 (넘으면 에러) |
| 5 | 상세 내용은 본문이 아니라 참조로 | 템플릿이 이미 규칙만 담게 되어 있다 |

## 스킬 예산

개인 스킬 description 합계 **2,000 토큰 이하**. 넘으면 새로 만들지 말고 **기존 스킬에 합친다.**
`promotion.py`의 `render_skill`은 같은 명령으로 묶인 코드들을 이미 자동으로 병합한다.

## 감가상각

- 30일 0회 호출 → 리포트에 삭제 후보로 표시
- 90일 0회 호출 → 삭제 제안, 승인되면 `~/.claude/skills/<이름>/` 폴더를 지운다

## 작성 규칙

- 본문은 **행동 규칙만.** 배경 설명·정당화·예시 나열은 뺀다.
- 규칙은 금지형보다 범위형으로: "전체를 보지 마라"(X) → "지목한 파일과 그 style 파일만 읽는다"(O)
- `promote`가 성공하면 `text-optimizer skill이 설치돼 있습니다` 줄이 출력에 뜨는지 본다
  (`text_optimizer_available()`이 파일로만 확인하는 것 -- 모델 호출 없음). 뜨면 방금 만든
  파일에 그 skill을 실제로 불러 적용할지 판단한다. 우리 템플릿은 이미 예산(500자/60줄)에
  비해 짧게 쓰여 있어서 압축 여지가 작을 때가 많다 -- 도움이 될 만큼 긴 파일에만 부른다.

## 효과 판정 상세

`promote`가 승격 시점의 발생률(baseline)을 원장에 같이 남기므로, 14일 뒤부터 `report`가
알아서 "승격한 명령어, 효과가 있었나" 절에 판정을 보여준다.

- **효과 있음**: 하루 발생률이 승격 전보다 30% 이상 줄었다 → 계속 쓴다.
- **사용 안 함**: 30일이 지났는데 실제 호출이 0~1회다 → 습관이 우연히 줄었어도 삭제 후보.
- **효과 없음**: 습관이 거의 줄지 않았다 → 삭제 후보. 리포트가 폴더 경로까지 알려준다.
- **판단하기엔 이르다**: 14일이 안 지났다 → 기다린다.

**직접 셀 필요 없다.** 사람이 할 일은 리포트를 읽고 지우라면 지우는 것뿐이다.
효과가 없는데도 안 지우면 상시 비용(description을 매 프롬프트마다 문다)만 남고
절감은 없는 상태가 방치된다 — 승격만 하고 안 지우는 것이 정확히 SkillReducer가 경고한 실패다.
